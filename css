:root {
    --color-text-main: #ececeb;
    --color-background: #080808;
    --color-border: #ececeb;
    --color-link: #ececeb;
    --color-link-hover: #ffffff;
    --main-container-max-width: 95%;
    --padding-general: 20px;
    --border-width: 2px;
}
html, body {
    height: 100%;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    position: relative;
}
/* Background image for body */
body::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image: url('https://i.imgur.com/aEKWNWp.png');
    background-repeat: repeat;
    background-size: 200px 200px;
    opacity: 0.5;
    z-index: -1;
}
/* Dark overlay */
body::after {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: rgba(0, 0, 0, 0.8); /* Adjust alpha for darkness */
    z-index: -1; /* Ensure overlay is above the background */
}
/* Text hover effects */
h1:hover, h2:hover, h3:hover, p:hover, a:hover, span:hover {
    text-shadow: 0 0 1px #ececeb, 0 0 2px #ececeb;
    transition: text-shadow 0.2s ease, color 0.2s ease;
}
a {
    text-decoration: none;
    color: #ececeb;
}
a:hover {
    color: #ffffff;
}
/* Header Styles */
.header-container {
    width: 100%;
    height: 200px;
    background: url('https://i.imgur.com/7oZfSgP.jpeg') no-repeat center top / cover;
    padding: 20px;
    display: flex;
    justify-content: space-between; /* Space between logo and title */
    align-items: center;
    text-align: center;
    position: relative;
    overflow: hidden;
    z-index: 0; /* Ensure other elements don't overlap */
}
/* Darker overlay for the header */
.header-container::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: rgba(9, 9, 9, 0.8); /* Darker overlay */
    z-index: -1; /* Keep overlay behind content */
}
.website-title {
    flex: 1;
    text-align: center; /* Center the website title */
}
.image-container img {
    width: 200px; /* Adjust size as needed */
    display: block;
    margin: 0 auto; /* Center the image */
    border-radius: 12px; /* Optional rounding for image corners */
    box-shadow: 0 0 15px #ECECEB; /* Optional shadow */
}
/* Main content container for layout */
.main-content {
    flex-grow: 1;
    width: 100%; /* Full width */
    max-width: none; /* Remove max-width limit */
    padding: 20px; /* General padding */
    box-sizing: border-box;
    display: flex;
    justify-content: center; /* Center content horizontally */
    align-items: center; /* Center content vertically if desired */
    min-height: 1300px; /* Ensure a minimum content height of 1300px */
}
/* TV container with relative positioning */
.tv-container {
    position: relative;
    top: -440px; /* Move it closer to the header (adjust as needed) */
    left: 400px; /* Reset the right position */
    width: 900px; /* Adjust width to fit the TV */
    height: 350px; /* Adjust height to fit the TV */
    display: block;
    margin-bottom: 20px; /* Space between the TV and recommendations */
}
/* TV image styling (z-index in front of the screen) */
.tv-image {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: auto;
    z-index: 2; /* Bring TV image in front of the screen */
    pointer-events: none; /* Allow clicks to pass through to the screen */
}
.tv-screen {
    position: absolute;
    top: 70px; /* Adjusted to match the position of the TV screen */
    left: 70px; /* Adjusted to match the left position of the TV screen */
    width: 600px; /* Adjusted width to fit the video */
    height: 470px; /* Adjusted height to fit the video */
    background-color: #000; /* Optional: dark background to simulate screen */
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    z-index: 1; /* Screen behind the TV image */
}
.recommendations-container {
    display: flex; /* Align items in a row */
    flex-direction: row; /* Set to row for horizontal alignment */
    justify-content: flex-start; /* Align items to the left */
    position: relative; /* Make sure the parent is positioned relative */
    margin-top: 750px; /* Space above the recommendations */
    left: -430px; /* Move it further to the left */
}
.anime-recommendation,
.korean-recommendation {
    text-align: center; /* Center align text under the image */
    margin: 0 20px; /* Space around the recommendations (left/right) */
}
.retro-border {
    border: 10px solid #ececeb; /* Light gray border */
    border-radius: 15px; /* Rounded corners */
    padding: 5px; /* Space between the image and the border */
    box-shadow: 0 0 15px rgba(0, 0, 0, 0.5); /* Add some shadow for a retro feel */
    width: 300px; /* Adjust width as necessary */
    height: auto; /* Keep the aspect ratio */
    transition: transform 0.3s; /* Smooth scaling effect */
}
.retro-border:hover {
    transform: scale(1.05); /* Scale effect on hover */
}
/* Footer Styles */
.footer-container {
    width: 100%;
    margin-top: 20px;
    background-color: rgba(0, 0, 0, 0.9); /* Darker background */
    color: #ececeb;
    padding: 30px 0;
    text-align: center;
    position: relative;
    z-index: 1;
    flex-shrink: 0;
}
.footer-container a {
    color: #ececeb;
    text-decoration: none;
    transition: color 0.3s ease;
}
.footer-container a:hover {
    color: #ffffff;
}
.copyright {
    font-size: 0.9em;
    color: #ececeb;
}
